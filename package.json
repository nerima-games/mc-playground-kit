{
  "name": "@nerima-games/mc-playground-kit",
  "version": "0.1.0",
  "type": "module",
  "description": "Dev-only preview harness for the nerima-games Minecraft-clone rebuild: launch options, boot phase budget, and the glue that stands up a mini world + camera + renderer + input in about a second.",
  "homepage": "https://github.com/nerima-games/mc-playground-kit#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nerima-games/mc-playground-kit.git"
  },
  "bugs": {
    "url": "https://github.com/nerima-games/mc-playground-kit/issues"
  },
  "author": "takeokunn",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "packageManager": "pnpm@11.17.0",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "files": [
    "index.ts",
    "domain",
    "application",
    "tsconfig.base.json",
    "LICENSE",
    "README.md"
  ],
  "keywords": [
    "minecraft",
    "effect-ts",
    "typescript",
    "voxel",
    "playground",
    "preview"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.build.json --pretty false && tsc -p tsconfig.test.json --pretty false && tsc -p tsconfig.preview.json --pretty false",
    "lint": "oxlint --deny-warnings index.ts domain application scripts test apps",
    "lint:fix": "oxlint --fix index.ts domain application scripts test apps",
    "preview": "tsx apps/preview-harness/main.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check:deps": "tsx scripts/check-dependency-whitelist.ts",
    "api:check": "tsx scripts/api-lock.ts",
    "api:update": "tsx scripts/api-lock.ts --write",
    "verify": "pnpm typecheck && pnpm lint && pnpm check:deps && pnpm api:check && pnpm test"
  },
  "dependencies": {
    "effect": "^3.22.0"
  },
  "devDependencies": {
    "@effect/vitest": "^0.30.0",
    "@typescript/native": "npm:typescript@7.0.2",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^3.2.7",
    "oxlint": "^1.76.0",
    "tsx": "^4.23.1",
    "typescript": "npm:@typescript/typescript6@6.0.2",
    "vitest": "^3.2.7"
  }
}
